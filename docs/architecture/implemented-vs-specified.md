# Implemented vs Specified

**Status:** runtime honesty for `main`. Fail-closed: a library, test harness, OpenAPI path, or dispatch-preparer is **not** a live capability.

**Grounding:** Enterprise architecture gap analysis (2026-09-21, review SHA `e4d0c9d`, finding **I3**) plus current code and docs. Epic G / phase G.0. This page is G.0.2.

This repository is a **security-hardened prototype with a production veneer**, not an enterprise-ready platform. Specified contracts stay normative for how the product *must* work when a slice ships. They do not mean that slice runs today.

## Hard lines (unchanged)

Do not weaken these because a capability is unimplemented:

| Line | Rule |
| --- | --- |
| YAML SoT | `flowforge/v1` is the only persisted workflow definition. Canvas is a projection. Invalid YAML never guesses a graph. |
| Drafts never run | Publish (or an explicitly approved published test version) then start. Organizing or editing a draft does not make it live. |
| Vault | Display-name + UUID only in chrome, search, and APIs. No KEK, PEM, password, or secret material in browser, room, YAML, logs, or `localStorage`. |
| ADV-021 | Embed chrome from `GET /session` `session.embed` only. Host query is display-only. Missing bind is a visible alert. |
| ADV-024 | Membership / isolation stay grant-gated. Isolation success is a **denial**. |
| Fail-closed authz | Server-derived workspace scope. Host-supplied workspace IDs are `400`. Cross-workspace UUIDs are `404`. |
| Not an n8n clone | Parity of interaction and coverage, not pixels, orange, or a marketplace. |

## Vocabulary

| Status | Meaning |
| --- | --- |
| **Runs today** | Shipped on the default compose or API path and used by operators or hosts. |
| **Partial** | API, chrome, or library exists; the production caller, scheduler, or provider call does not. |
| **Specified only** | Documented contract and/or in-repo engine. Nothing in a shipped deployment invokes it for real work. |
| **Deferred** | Explicitly not this phase (named stub or later epic). |

## Matrix

| Area | Specified | Runs today | Status |
| --- | --- | --- | --- |
| **Production provider worker** | Isolated, production-locked worker pods execute Kubernetes, SSH, signed scripts, and HTTP one step at a time after revalidating job/version/policy/lease ([architecture](../architecture.md), engines). | `apps/api/cmd/runner` is production-locked and dispatches to the existing engine packages. Compose `apps/api/cmd/worker` still refuses provider nodes and refuses to start when production-locked. `deploy/k8s` runs the runner, not `/usr/local/bin/worker`. Default-deny egress does not open provider networks. | **Partial** (G.1.1) |
| **Kubernetes / SSH / script / HTTP nodes** | `kubernetes.apply` / `get` / `list` / rollout, `ssh.run`, `script.python` / `script.go`, `http.request` contact real targets from a worker. | The production runner calls those engines after a published pin and HMAC fence. Compose still fails the step (`local-worker-unsupported`). Live dials fail at the network until an operator allowlist exists. `notification.email` fails closed without a mailer. `script.python` / `script.go` create an isolated Job from `deploy/kubernetes/script-runner-deployment.yaml` using `ghcr.io/bbengt1/flowforge-script-runner` (built from `apps/api/Dockerfile.script-runner`). The script NetworkPolicy allows DNS plus `CONTROL_PLANE_API_CIDR` only; a missing CIDR or a policy that does not match refuses the Job. | **Partial** |
| **Compose local worker** | Dev claim loop so **Start published** can leave `queued`, same lease / HMAC ticket / fencing as any worker ([deployment](../deployment.md#local-compose-worker)). | **Runs locally** for six core nodes: `flow.condition`, `flow.stop`, `flow.fail`, `data.set`, `data.map`, `data.validate`. `flow.approval` is parked `waiting` by the API (worker skips). `flow.delay` and every provider node fail closed. Not a Kubernetes workload. | **Partial** (core eval only) |
| **Schedules / dispatch** | Timezone-explicit schedules fire published versions; `POST /api/v1/schedules/dispatch` is the tick. | CRUD, enable/disable, and the dispatch endpoint exist. The API process leader (`SCHEDULER_ENABLED`, default on, advisory lock `881726402`) calls that same dispatch path on an interval. Only the leader ticks. Drafts and unpublished pins are skipped. A worker is still required for the started run to leave `queued`. | **Runs today** (G.1.3) |
| **Lease recovery** | Expired claims become `indeterminate` via fencing; `POST /api/v1/jobs/recover`. | The same leader calls recover on the interval. The endpoint remains. A stale HMAC `jobToken` still cannot complete. | **Runs today** (G.1.3) |
| **Retention purge / backups** | 90-day execution / 365-day audit purge; encrypted `pg_dump`. | The leader calls retention purge on the same interval (legal holds stay). `deploy/k8s/backup-cronjob.yaml` runs `scripts/backup/run-encrypted-backup.sh` daily (logical RPO 24h) and writes an FFB1 integrity manifest. `flowforge-wal-archive` seals WAL (PITR RPO 300s) and `flowforge-pitr-base` seals a physical base backup. Ciphertext lands in `BACKUP_S3_BUCKET` under `flowforge-db/`. Rehearsals are `scripts/backup/rpo-rto-rehearsal.sh` and `scripts/backup/manifest-pitr-rehearsal.sh` in E12.2 CI. | **Runs today** (purge G.1.3; backups G.1.4 ; manifest + PITR G.2.7) |
| **OIDC / SSO / MFA / SCIM** | Enterprise identity; architecture mentions host/OIDC subjects. | OIDC Authorization Code + PKCE (`POST /oidc/start`, `POST /oidc/callback`), TOTP MFA on `platform.administer` and `credential.*`, SCIM 2.0 Users/Groups (`/scim/v2`, dedicated bearer), and durable `auth_lockouts` are API contracts. No SAML. Chrome follows separately. | **Partial** (gap **A2** API; SAML still absent) |
| **Standalone Login** | Local email/username + password mints `ff_session` / `ff_csrf`. | **Runs today.** `POST /api/v1/login`. First-run one-time `admin` / `admin` sets `must_change_password` until `POST /api/v1/session/password`. Never on `/embed/v1`. Trusted-dev `POST /session` is local/dev only — not the product door. | **Runs today** |
| **Embed exchange** | Host backend exchanges a signed assertion; chrome reads `session.embed` only (ADV-021). | **Runs today.** `POST /embed/exchange`. Embed never mounts Login, the first-run wizard, or change-password. Host `?tenant=` / `?workbench=` stay display-only. | **Runs today** |
| **Machine principal** | Non-human `client_id` + secret or signed assertion mints the same `ff_session` authz path for scrapers, scheduler, and automation. | **Runs today.** `POST /api/v1/machine/token` plus create/rotate/revoke under `platform.administer`. Grants deny-by-default (`ops.metrics.read` is explicit, not a workspace role). Secret is never echoed. Distinct from Login, trusted-dev, and embed exchange. `MACHINE_REQUIRE` fails closed when a named consumer is missing. | **Runs today** (G.1.6) |
| **Bootstrap / TLS skip** | Standalone first-run wizard: persistence → admin → public URL → TLS (create, upload, or skip). | **Runs today (B.1–B.8).** Incomplete → wizard only. Complete or `401` → Login, not wizard. **Skip for now** POSTs `{action:"skip"}` (no PEM); instance stays HTTP until Settings. Localseed / migrate backfill can mark complete + skipped. **Never on `/embed/v1`.** ACME is out of scope. | **Runs today** |
| **Explorer chrome** | `/workflows` Explorer: server-backed folder tree, content pane, breadcrumb, grant-gated menus (F.1–F.7, X.1–X.8). | **Runs today (chrome + API).** Same `WorkflowHome` on standalone and `/embed/v1/workflows` after `session.embed`. Tree is not `localStorage`. Folder membership is **not** in YAML. This is organizer chrome, not provider execution. | **Runs today** |
| **Workflow soft-delete** | Delete hides a workflow, unpublishes it, disables triggers and schedules, and reserves the slug. No hard purge and no restore. | **Runs today (API + web).** `DELETE /api/v1/workflows/{workflowId}` returns `204`. Editors, workspace admins, and the workflow owner may delete. Viewers who are not the owner get `403`. No view access gets `404`. The delete handler refuses any embed session with `403` `forbidden` before permission, capability, or ownership checks, including a session whose stored caps still list `workflow.delete`. `capabilities.delete` is false for every embed session. Ownership does not add a permission beyond the embed caps. `workflow.delete` is not mintable. A non-terminal run with a job in `queued`, `claimed`, or `running` gets `409` `workflow_has_active_executions` and is not changed, including a gate parked beside a sibling job still in flight. A run whose only unfinished work is `waiting`, `pending`, or `blocked` does not block: delete cancels those steps and jobs, closes pending approvals with `closeReason` `workflow_deleted`, and fails the run with `workflow_deleted` in the same transaction, which frees the concurrency slot. An active run whose latest jobs are already finished is rolled up to that terminal state in the same transaction, which also frees the slot. Workflow detail for a caller who can delete includes `deleteImpact` (`waitingRuns`, `blocked`, `inFlightRuns`); viewers and embed sessions omit it. A later resume of a run that was still open finds the tombstone and fails with `workflow_deleted`. Triggers and schedules of a tombstone are `404` and stay disabled. A reserved explicit slug gets `409` `workflow_slug_reserved` with `errors` path `slug`. Create chooses the slug from the JSON body, then `metadata.slug`, then a derivation of the display name, and writes that slug back into `metadata.slug`. Only a derived slug gains a numeric suffix when the unique index clashes, including a tombstone. An explicit clash is not rewritten. Workflow home shows that derivation as an estimate (Suggested slug). The server may add a numeric suffix after trimming the base to the length cap, so the estimate is not sent unless the operator edits the Slug field. After create, the notification and the editor show the slug from the create response. Starter template YAML has no `metadata.slug`. A slug `conflict` (`slug already exists`) and `workflow_slug_reserved` are shown on the Slug field. Other 409s, including `execution_not_retryable`, `step_attempt_superseded`, `approval_closed`, and a generic unique violation, are not. No delete route on `/embed/v1`. The confirm UI is the Explorer context menu, the Explorer selection action, and the editor overflow menu. It renders only when `capabilities.delete` is true (a missing flag is hidden) and is also absent on `/embed/v1`. Success drops the workflow from the list, Explorer, and search caches. A deleted deep link uses the workflow not-found boundary. | **Runs today** (API + web) |
| **Web route boundaries** | App Router `error` / `loading` / `not-found` on primary segments; retry on error (gap F1). | **Runs today.** Root `global-error.tsx` + segment `error.tsx` with Try again. `loading.tsx` and `not-found.tsx` on primary product segments. Embed ADV-021 fail-closed unchanged. | **Runs today** (gap **F1**; G.0.4) |
| **Realtime channels** | Workspace-scoped realtime for execution visibility. | API has subscribe routes. **UI polls ~2s when the tab is visible** (backoff/jitter; hidden tabs pause). `EventSource` / `WebSocket` are forbidden in current chrome tests. | **Specified only** (gap **A4**; G.0.12 gates polling) |
| **HMAC job / script keys** | Every boundary fails closed; production secrets required. | `JOB_BINDING_SECRET` and `SCRIPT_SIGNING_KEY` are **required at boot**. Missing or malformed refuses to start (no per-process random default). Compose/dev uses documented local-only values so restarts stay stable. `EMBED_SIGNING_KEY` already boot-fails in production. | **Runs today** (G.0.3 gap **B1**) |
| **Artifact durability** | Queue, leases, execution state, audit, and artifact metadata survive pod loss. | Metadata is in PostgreSQL. Payloads are envelope-encrypted and written to `{tenant}/{workspace}/{ref}` in a configurable S3-compatible store (compose MinIO on `minio_data`). Production-locked processes boot-fail without bucket and credentials (no directory fallback). Draft executions cannot attach run artifacts. | **Runs today** (G.1.5 gap **B3**) |
| **OpenAPI / tracing (E1.2)** | Generated OpenAPI; API-to-worker flows traced by correlation ID. | OpenAPI is **hand-written**. `X-Request-ID` exists; **no trace store, sampling, or worker span propagation.** `GET /api/v1/health` now publishes non-secret `version` / `sha` (G.0.10 C1 build identity). | **Partial** (gaps **B5**, **C1**) |
| **DB session timeouts** | `statement_timeout` / `lock_timeout` on checkout so one query cannot pin the pool. | Application-pool `PrepareConn` (pgx v5 checkout) sets `15s` / `5s` (`STATEMENT_TIMEOUT` / `LOCK_TIMEOUT`). Circuit breakers and worker/UI bulkheads are still out of scope. | **Partial** (gap **C2**; G.0.11) |
| **HA / web on Kubernetes** | Isolated worker pods + UI in cluster. | `deploy/k8s` runs API, web (`flowforge-web`, `GET /` probes), and runner at `replicas: 2` with PDB (`maxUnavailable: 1`), HPA (minimum 2), preferred hostname anti-affinity, `preStop`, and termination grace. SIGTERM resigns the scheduler lease before HTTP drain. `JOB_BINDING_SECRET` and `SCRIPT_SIGNING_KEY` are the shared Secret. Compose `/usr/local/bin/worker` is not deployed. | **Runs today** (G.2.6 ; web G.1.7). A managed-Postgres manifest is still not in `deploy/k8s`. |

## Provider execution

`cmd/runner` is production-locked and calls the engine packages. Compose `cmd/worker` does not.

- Do say: the production runner dispatches `kubernetes.*`, `ssh.run`, `script.python` / `script.go`, and `http.request` / `notification.webhook` after a published pin, HMAC ticket, and fence.
- Do say: compose still fails those nodes with `local-worker-unsupported`.
- Do **not** say clusters, SSH hosts, or webhooks are reachable from `deploy/k8s` until an operator adds that provider egress. Script Jobs are created only after `CONTROL_PLANE_API_CIDR` (or a same-namespace Service) is set and `flowforge-script-runner` allows DNS plus that destination. Default-deny stays. A world CIDR is rejected.
- Engine references ([Kubernetes](../reference/kubernetes-engine.md), [SSH](../reference/ssh-engine.md), [script](../reference/script-engine.md)) describe the libraries the runner calls.

Executable on compose today: **6 of 18** defined node types (`flow.condition`, `flow.stop`, `flow.fail`, `data.set`, `data.map`, `data.validate`). Non-executable on that worker: provider nodes, `flow.delay`, plus registry-disabled `data.filter` / `data.merge` / `data.sort` / `flow.join` / `flow.parallel` / `flow.switch`. `flow.approval` waits in the API. The production runner also evaluates those six core nodes, parks `flow.approval` and inserts the approval in that same transaction with `expires_at` copied from the wait deadline, and parks `flow.delay` with `WaitJob` until the duration elapses (not an in-process sleep). Gate expiry closes that approval in the same transaction. Decide after the gate is no longer `waiting` is `409` `approval_closed` and does not record an approval. Compose still fails `flow.delay`. A step retry is refused unless the run is `failed` or `indeterminate` and the latest attempt of that step started and ended `failed` or `indeterminate`. A soft-deleted workflow and an SSH or script step that is not retry-safe both refuse with `execution_not_retryable` (`workflow_deleted` or `retry_not_allowed`). Cancel and `workflow_deleted` close pending approvals in the same transaction. Memory cancel closes pending approvals with `run_canceled` as well.

## Identity doors (do not merge)

| Door | Who | Endpoint | Notes |
| --- | --- | --- | --- |
| Standalone Login | Operator | `POST /api/v1/login` | Local password. Product door after bootstrap complete. |
| Embed exchange | Host backend | `POST /embed/exchange` | ADV-021. Never Login chrome. |
| Trusted-dev session | Local/dev only | `POST /api/v1/session` + identity headers | Fail-closed outside non-prod + `TRUSTED_DEV_IDENTITY_HEADERS`. Not rewrite login. |
| Machine principal | Scheduler, scrapers, automation | `POST /api/v1/machine/token` | Same `ff_session` authz. Server-side secret. Not Login, trusted-dev, or embed exchange. |
| OIDC + MFA | Enterprise IdP | `POST /api/v1/oidc/start`, `POST /api/v1/oidc/callback`, `GET/POST /api/v1/session/mfa` | Authorization Code + PKCE mints the same `ff_session`. TOTP gates privileged grants on local-login and OIDC only. No IdP-admin API. A locked `auth_lockouts` row blocks the callback with the same `401` as a bad code. |
| SCIM 2.0 | Directory provision | `/scim/v2/Users`, `/scim/v2/Groups` | Dedicated `SCIM_BEARER_TOKEN`. Maps into `users` and workspace roles. Not Login, machine token, or embed exchange. Groups are existing workspaces. |

## What this page is not

- A license to weaken drafts-never-run, vault metadata-only, ADV-021/024, or YAML SoT.
- A substitute for the gap analysis findings (A–I) or the E1–E12 [master implementation plan](../master-implementation-plan.md).
- A claim that later identity work has shipped. The production runner (G.1.1), leader-elected scheduler (G.1.3), encrypted backup CronJob (G.1.4), durable artifact storage (G.1.5), machine/service-principal credentials (G.1.6), and web Deployment (G.1.7) have. Backup integrity manifests and WAL/PITR archiving (G.2.7) have. OIDC Authorization Code + PKCE and TOTP MFA (G.2.1 API) have. SCIM 2.0 provision/deprovision and durable account lockout (G.2.2 API) have. SAML has not. Login and MFA chrome follow separately.

When a later story makes a row **Runs today**, update this table in the same PR as the code.
